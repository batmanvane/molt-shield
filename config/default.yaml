server:
  host: "127.0.0.1"
  port: 3000

masking:
  value_pattern: "-?\\d+\\.?\\d*"
  uuid_format: "VAL_{uuid}"
  preserve_attributes: ["id", "type"]

shuffling:
  enabled: true
  seed: null
  target_tags: ["element", "node", "component"]

vault:
  path: "./session_vault.json"

strict_mode: false
